id: expressionengine-detect

info:
  name: ExpressionEngine Detection
  author: Chirag Mistry
  severity: info
  description: |
    An instance running ExpressionEngine is detected by looking for specific HTTP headers, HTML tags, and known endpoints.
  metadata:
    max-request: 9
  tags: expressionengine,detect,tech

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/robots.txt"
      - "{{BaseURL}}/sitemap.xml"
      - "{{BaseURL}}/favicon.ico"
      - "{{BaseURL}}/system/"
      - "{{BaseURL}}/themes/"
      - "{{BaseURL}}/index.php"
      - "{{BaseURL}}/admin.php"
      - "{{BaseURL}}/cp/"

    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: word
        part: response
        words:
          - "ExpressionEngine"
          - "Powered by ExpressionEngine"
          - "expressionengine.com"
          - "X-Powered-By-ExpressionEngine"
        condition: or
        case-insensitive: true

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        name: version
        regex:
          - 'ExpressionEngine ([0-9.]+)'
          - 'Powered by ExpressionEngine ([0-9.]+)'

      - type: regex
        part: body
        name: expressionengine_id
        regex:
          - 'expressionengine-site-id=([a-z0-9]+)'
          - 'expressionengine\.com/([^/]+)'
# digest: 4a0a0047304502201e2f305438952ac46d9114835e4ec3d0d791c207c21b85da11b473e69b3411f9022100e4b6ad3c2f2b1f317e2b2e9d9f64301ece9b6be2d18ffc175c3f48a3f6457632:922c64590222798bb761d5b6d8e72950