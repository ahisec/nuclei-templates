id: expressionengine-detect

info:
  name: ExpressionEngine Detection
  author: Chirag Mistry
  severity: info
  description: |
    An instance running ExpressionEngine is detected by looking for specific HTTP headers, HTML tags, and known endpoints.
  metadata:
    max-request: 9
  tags: expressionengine,detect,tech

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/robots.txt"
      - "{{BaseURL}}/sitemap.xml"
      - "{{BaseURL}}/favicon.ico"
      - "{{BaseURL}}/system/"
      - "{{BaseURL}}/themes/"
      - "{{BaseURL}}/index.php"
      - "{{BaseURL}}/admin.php"
      - "{{BaseURL}}/cp/"

    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: word
        part: response
        words:
          - "ExpressionEngine"
          - "Powered by ExpressionEngine"
          - "expressionengine.com"
          - "X-Powered-By-ExpressionEngine"
        condition: or
        case-insensitive: true

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        name: version
        regex:
          - 'ExpressionEngine ([0-9.]+)'
          - 'Powered by ExpressionEngine ([0-9.]+)'

      - type: regex
        part: body
        name: expressionengine_id
        regex:
          - 'expressionengine-site-id=([a-z0-9]+)'
          - 'expressionengine\.com/([^/]+)'
# digest: 4a0a00473045022100e0fb025338cf331d093afa690af52f89a6576cb3f19a0d0d5fa50e84c4f4147102205c56fd21e4037a6ca0c05f18439ee8a707c9d01b58856b7351aa78409e8abcaa:922c64590222798bb761d5b6d8e72950