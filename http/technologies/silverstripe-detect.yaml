id: silverstripe-detect

info:
  name: SilverStripe Detection
  author: Chirag Mistry
  severity: info
  description: |
    An instance running SilverStripe is detected by looking for specific HTTP headers, HTML tags, and known endpoints.
  metadata:
    max-request: 9
    shodan-query: http.component:"SilverStripe"
  tags: silverstripe,detect,tech

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/robots.txt"
      - "{{BaseURL}}/sitemap.xml"
      - "{{BaseURL}}/favicon.ico"
      - "{{BaseURL}}/index.php"
      - "{{BaseURL}}/assets/"
      - "{{BaseURL}}/cms/"
      - "{{BaseURL}}/admin/"
      - "{{BaseURL}}/themes/"

    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: word
        part: response
        words:
          - "Powered by SilverStripe"
          - "X-Powered-By-SilverStripe"
        condition: or
        case-insensitive: true

      - type: status
        status:
          - 200
          - 403

    extractors:
      - type: regex
        part: body
        name: version
        regex:
          - 'SilverStripe ([0-9.]+)'
          - 'Powered by SilverStripe ([0-9.]+)'

      - type: regex
        part: body
        name: silverstripe_id
        regex:
          - 'silverstripe-site-id=([a-z0-9]+)'
          - 'silverstripe\.org/([^/]+)'
# digest: 4b0a004830460221008654234c0780ad0243b72f404c5ba2e6d5f44deffb380c8c3d02d2dec9552434022100c0f5b7d7a399fc2fb7f95cf297d10843ae6ef240f5df4d3ee3b2b151f473da83:922c64590222798bb761d5b6d8e72950