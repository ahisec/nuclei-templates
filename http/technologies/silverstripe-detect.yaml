id: silverstripe-detect

info:
  name: SilverStripe Detection
  author: Chirag Mistry
  severity: info
  description: |
    An instance running SilverStripe is detected by looking for specific HTTP headers, HTML tags, and known endpoints.
  metadata:
    max-request: 9
    shodan-query: http.component:"SilverStripe"
  tags: silverstripe,detect,tech

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/robots.txt"
      - "{{BaseURL}}/sitemap.xml"
      - "{{BaseURL}}/favicon.ico"
      - "{{BaseURL}}/index.php"
      - "{{BaseURL}}/assets/"
      - "{{BaseURL}}/cms/"
      - "{{BaseURL}}/admin/"
      - "{{BaseURL}}/themes/"

    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: word
        part: response
        words:
          - "Powered by SilverStripe"
          - "X-Powered-By-SilverStripe"
        condition: or
        case-insensitive: true

      - type: status
        status:
          - 200
          - 403

    extractors:
      - type: regex
        part: body
        name: version
        regex:
          - 'SilverStripe ([0-9.]+)'
          - 'Powered by SilverStripe ([0-9.]+)'

      - type: regex
        part: body
        name: silverstripe_id
        regex:
          - 'silverstripe-site-id=([a-z0-9]+)'
          - 'silverstripe\.org/([^/]+)'
# digest: 4a0a0047304502205860a3d53b16aeb8bb1d23bf33f67860dd525431a4db9b83e88ac2d066949711022100f85f6c17c8c4b0c2f561a5cc38010398ee3b4aee3c49fab9fdd2c7ec9696b045:922c64590222798bb761d5b6d8e72950