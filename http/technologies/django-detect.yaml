id: django-detect

info:
  name: Django Detection
  author: Chirag Mistry
  severity: info
  description: |
    An instance running Django is detected by looking for specific HTTP headers, HTML tags, and known endpoints.
  metadata:
    verified: true
    max-request: 9
    shodan-query: http.component:"django"
  tags: django,detect,tech

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/robots.txt"
      - "{{BaseURL}}/sitemap.xml"
      - "{{BaseURL}}/favicon.ico"
      - "{{BaseURL}}/admin/"
      - "{{BaseURL}}/static/"
      - "{{BaseURL}}/media/"
      - "{{BaseURL}}/index.php"
      - "{{BaseURL}}/django-admin/"

    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "Powered by Django"
          - "django-project"
          - "Django Software Foundation"
        condition: or
        case-insensitive: true

      - type: status
        status:
          - 200
          - 403
          - 404
        condition: or

    extractors:
      - type: regex
        part: body
        name: version
        regex:
          - 'Django ([0-9.]+)'
          - 'Powered by Django ([0-9.]+)'

      - type: regex
        part: body
        name: django_id
        regex:
          - 'django-site-id=([a-z0-9]+)'
          - 'django\.org/([^/]+)'
# digest: 4a0a00473045022100b610f4c7f2bc8e4aff9fbe82116857104f3b0509b0e2411467b8489e02eae9c5022057b3609cfb70c4a7abf538c53329e5ba69570cf7792caa48cd1672c8108b15f3:922c64590222798bb761d5b6d8e72950